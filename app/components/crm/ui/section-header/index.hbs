<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white">{{@title}}</h1>
  {{#if (has-block "action")}}
    <div>{{yield to="action"}}</div>
  {{/if}}
</div>
